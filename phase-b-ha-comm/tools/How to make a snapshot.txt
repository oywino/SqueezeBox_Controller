Superseded by README.md and docs/release-process.md.

Current project path inside the Ubuntu container:
/workspace/SqueezeBox_Controller

Current policy:
- Create snapshots/tags only after successful hardware verification.
- Keep snapshots and release archives out of normal Git history.
- Use annotated Git tags for verified milestones.
